#include <stdio.h>
using namespace std;

int main()
{
    int i, j, a, b = 0, t;
    scanf("%d", &t);
    while (t--)
    {
        scanf("%d", &i);;
        for (a = 3;a < i;a++)
        {
            if (a % 3 == 0 || a % 5 == 0)
            {
                b = b + a;
            }

        }
        printf("%d\n",b);
        b = 0;
    }
    return 0;
}
